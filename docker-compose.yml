services:
  db:
    image: postgres:16
    container_name: waytohome-db
    environment:
      POSTGRES_DB: waytohome
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - waytohome-data:/var/lib/postgresql/data
volumes:
  waytohome-data:
