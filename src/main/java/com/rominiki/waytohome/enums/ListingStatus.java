package com.rominiki.waytohome.enums;
public enum ListingStatus {
    PENDING,  // just created, awaiting admin review
    APPROVED, // visible in public search
    REJECTED  // hidden, admin rejected it
}